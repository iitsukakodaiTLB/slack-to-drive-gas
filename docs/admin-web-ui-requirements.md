# 管理用 Web UI 要件定義

## 文書の位置づけ

Slack ログ同期（GAS）の**運用・監視・限定的な設定変更**を、設定スプレッドシートを直接編集せずに行うための **Web アプリ（Google Apps Script Web アプリ）** に関する要件を定義する。

- **同期ワーカーのアルゴリズム・シート列定義の正本:** [slack-log-sync-design-proposal.md](./slack-log-sync-design-proposal.md)
- **本書のスコープ外:** リポジトリのディレクトリ構成、clasp の運用、同期ワーカー実装の詳細（別資料・README に委ねる）。

**ステータス:** 要件定義としての初版。実装は本書の合意（または明示的な差し替え）を前提とする。

---

## 1. 背景・目的

### 1.1 背景

- チャンネルごとの同期状態はスプレッドシート（想定名: `slack_log_to_drive_setting`）の `channel_sync_state` 等で管理される。
- 列数が多く、ステータス種別・日時・カーソル・ロック・エラー情報が混在するため、**シートを直接開いて編集する運用は負荷が高く、誤入力・列ずれのリスク**がある。

### 1.2 目的

- 運用担当が **ブラウザから安全に一覧・検索・閲覧**できるようにする。
- **運用で必要な範囲の項目のみ**を、入力支援付きで更新できるようにする（同期ワーカーが解釈する値の整合性を UI 側で担保しやすくする）。
- スプレッドシートの**生データ編集を日常運用から切り離す**（緊急時の直接編集は設計上禁止しないが、標準動線は Web UI とする）。

### 1.3 成功の目安（非機能とあわせたゴール像）

- 同期対象チャンネルの状態を、**シートを開かずに**把握できる。
- `DISABLED` 切り替えや `note` 更新など、**定義された操作以外は UI から行えない**（または権限・確認で抑止される）。
- Slack Bot Token 等のシークレットは **ブラウザに渡らない**。

---

## 2. 本書におけるスコープ

| 区分 | 内容 |
|------|------|
| **対象（In scope）** | Web UI の利用者、認証・認可、画面・機能、表示・更新対象データ、非機能（セキュリティ・性能・保守上の制約）、用語と参照関係 |
| **対象外（Out of scope）** | Git / clasp / フォルダ構成、同期ワーカーのトリガー間隔・API 呼び出しロジック、Drive 上ログファイルのフォーマット詳細（設計書を参照） |

---

## 3. 前提（システム上の位置づけ）

| 項目 | 内容 |
|------|------|
| デプロイ | **同期ワーカー用 GAS とは別プロジェクト**のスタンドアロン GAS として Web アプリを公開する。 |
| データアクセス | 設定スプレッドシート等は **サーバー側 GAS**（`SpreadsheetApp` 等）から読み書きする。クライアントは `google.script.run` 等でサーバー関数を呼び出す。 |
| シークレット | Slack Token、スプレッドシート ID 等は **Script Properties**（または GAS 側の安全な設定）に保持し、**HTML/クライアント JS に埋め込まない**。 |

**要決定事項（実装前に確定が必要）:** Web アプリを「誰の権限で」スプレッドシートにアクセスさせるか（デプロイユーザーの Google アカウントとスプレッドシートの共有関係）。本書では具体的なアカウント名は固定しない。

---

## 4. 利用者・認証・認可

### 4.1 利用者

- **主利用者:** 当該 Google ワークスペースにおける**運用担当者**（同期の監視・優先度調整・無効化などを行う人）。
- **非利用者（初期）:** 一般インターネットユーザー、Slack 上の通常メンバー（Slack アカウントだけでは UI に入れない想定）。

### 4.2 認証

- **Google アカウントによるアクセス**を前提とする（GAS Web アプリのデプロイ設定と整合させる）。
- **オプション（推奨を要件化するか要判断）:** 許可されたメールアドレスのみアクセス可能とする**許可リスト**（Script Properties または設定シート）。リスト外は URL を知っていても利用不可とする。

### 4.3 認可

- 初期リリースでは **ロール分離（管理者／閲覧のみ等）は必須としない**（単一ロールでよい）。将来、閲覧専用ロールを追加する余地は残す。

---

## 5. 機能要件

### 5.1 データ範囲（参照の正）

- **一覧・詳細の主対象:** `channel_sync_state` シートの行（1 行 = 1 チャンネル）。列の意味・値域は設計書 [slack-log-sync-design-proposal.md](./slack-log-sync-design-proposal.md) の「§4. `channel_sync_state` シート列」に従う。
- **`thread_queue`:** MVP では**一覧の参照のみ**または**非表示**とする（フル CRUD は後続）。必要なら件数サマリ等は別途検討。
- **`slack_user_cache`:** MVP では **UI からの編集対象外**（同期ワーカーが更新する）。

### 5.2 画面構成（必須）

| ID | 画面 / 機能 | 要件 |
|----|-------------|------|
| S-01 | **チャンネル一覧** | `channel_sync_state` の全行（またはページ分割）を表形式で表示する。 |
| S-02 | **フィルタ・ソート** | 少なくとも `status` での絞り込み、および「エラーあり」（例: `last_error_at` が空でない、または `status === ERROR`）の絞り込みができること。ソートキーは `channel_name_current` / `live_last_message_at` / `sort_last_run_at` から MVP で選択してよい。 |
| S-03 | **チャンネル詳細** | 一覧から 1 チャンネルを選び、当該行の**全列**（または運用に必要な列を明示したサブセット）を表示する。設計書上の意味が分かりにくい列には UI ラベルまたはツールヒントで補足する。 |
| S-04 | **手動更新** | 次節「更新可能列」で許可された列のみ、フォーム等で編集し保存できる。保存時はバリデーション（値域・日時形式・列名整合）を行う。 |
| S-05 | **フィードバック** | 保存成功・失敗をユーザーに明示する。失敗時は可能な範囲で理由（権限、競合、バリデーション）を表示する。 |

### 5.3 更新可能列（MVP 案 ※合意が必要）

同期ワーカーと競合しないこと、および誤操作の影響度を踏まえ、**初期は次の範囲を推奨**とする。

| 列名 | MVP での扱い | 備考 |
|------|----------------|------|
| `status` | **編集可（制限付き）** | `DISABLED` と `WAITING`（または `PENDING`）等、**許可した遷移のみ** UI で選択可能にする。`RUNNING` への手動設定は原則禁止（ワーカー専用）。 |
| `priority_interrupt_at` | **編集可** | 日時クリア・設定。 |
| `note` | **編集可** | 自由記述。 |
| `channel_name_current` | **原則読み取り専用** | 表示用。ワーカーが Slack API で更新するため、手編集はオプション化または非推奨。 |
| `lock_owner`, `lock_until` | **読み取り専用** | 運用トラブルシュート用に詳細画面では表示するが、MVP では UI から更新しない。 |
| その他のカーソル・fileId 列 | **読み取り専用** | 同上。 |

上表は**確定案ではない**。承認時に「編集可にする列」を増減する。

### 5.4 明示的な非機能（機能面）

- **新規チャンネル行の追加**（シートへの行 INSERT）を MVP で行うかは**別途決定**（行追加は同期ワーカー側の登録フローと整合が必要）。
- **Slack API の直接呼び出し**（チャンネル検索等）は MVP では行わない。
- **Drive 上の CSV/JSONL の内容表示・ダウンロード**は MVP では必須としない。リンク表示のみ将来検討可。

---

## 6. 非機能要件

| ID | 項目 | 要件 |
|----|------|------|
| NF-01 | セキュリティ | シークレットをクライアントに渡さない（§3）。HTTPS で配信される GAS Web アプリの標準に従う。 |
| NF-02 | 性能・制限 | GAS の 1 回実行時間・回数制限内で完結する設計とする。一覧は**ページング**または**最大行数上限**を設け、全件一括読み込みでタイムアウトしないこと。 |
| NF-03 | 同時更新 | 同一行をシート上で直接編集された場合と Web UI から保存した場合の**最終書き勝ち**は許容する。必要なら保存前に `sort_last_run_at` 等の簡易チェックを将来検討（MVP では必須としない）。 |
| NF-04 | 可用性 | GAS / Google の障害・メンテナンスに依存する。独自 SLA は定めない。 |
| NF-05 | 言語・表示 | UI 文言は**日本語**を主とする。日時は運用者が理解しやすい形式（タイムゾーン方針は実装時に固定して文書化）。 |
| NF-06 | 保守 | 列名は設計書・同期ワーカーの `Config.js` と**一致させる**こと。変更時は同期ワーカーと同時リリースまたは後方互換の移行手順を定める（詳細は実装計画で扱う）。 |

---

## 7. UI / 実装方針（技術レベル）

- **HTML Service** による Web アプリ、`doGet` で HTML を返す構成とする。
- データ取得・更新は **`google.script.run` によるサーバー関数呼び出し**を基本とする（クライアントのみで完結する処理は最小限）。
- 見た目のフレームワーク・コンポーネントは**必須としない**（実装フェーズで選定）。アクセシビリティは可能な範囲で考慮するが、WCAG 準拠を MVP で必須とはしない。

---

## 8. 用語・参照

| 用語 | 説明 |
|------|------|
| 同期ワーカー | 時間主導トリガー等で Slack から Drive へログを取り込む GAS。 |
| `channel_sync_state` | チャンネルごとの同期状態を保持するシート（設計書 §4）。 |
| Web UI（本アプリ） | 本要件の対象となる管理用 Web アプリ。 |

---

## 9. 未決定事項・承認後の次アクション

### 9.1 要件上の未決定（承認前に埋める）

1. **§5.3** の「更新可能列」と `status` の**許可遷移**の最終リスト。
2. **§4.2** の許可リストを必須とするか。
3. **§5.4** の新規チャンネル行追加を MVP に含めるか。
4. **§3** のスプレッドシート共有・実行ユーザー（技術的セットアップ）。

### 9.2 承認後（本書の範囲外の作業）

- 画面ワイヤ・API（サーバー関数）の I/F 設計、実装、テスト、デプロイ手順の整備。

---

## 10. 変更履歴

| 日付 | 内容 |
|------|------|
| 2025-03-27 | たたき初版 |
| 2025-03-27 | リポジトリ・clasp をスコープ外化、Web UI 要件として再構成（画面・更新列・非機能の整理） |
